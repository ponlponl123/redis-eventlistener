{
  "dependencies": {
    "ioredis": "^5.6.0",
    "mariadb": "^3.4.1"
  },
  "name": "pona-redis-eventlistener",
  "author": "Ponlponl123",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --outfile=index.js --minify",
    "start": "node dist/index.js",
    "dev": "bun run src/index.ts",
    "lint": "bun run lint",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}